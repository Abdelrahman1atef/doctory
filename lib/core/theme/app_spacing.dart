/// App-wide spacing and padding constants.
class AppSpacing {
  AppSpacing._();

  /// Default bottom padding for screens to avoid overlap with navigation bar.
  static const double screenBottomPadding = 24.0;

  /// Standard horizontal padding for most screens.
  static const double horizontalPadding = 16.0;
}
